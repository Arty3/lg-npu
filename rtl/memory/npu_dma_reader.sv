// ============================================================================
// npu_dma_reader.sv - DMA read engine (stub - not used in current scope)
// ============================================================================

module npu_dma_reader
    import npu_types_pkg::*;
(
    input  logic clk,
    input  logic rst_n
);

    // Intentionally empty - placeholder for future external DRAM reads.

endmodule : npu_dma_reader
